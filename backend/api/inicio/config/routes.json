{
  "routes": [
    {
      "method": "GET",
      "path": "/inicio",
      "handler": "inicio.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/inicio",
      "handler": "inicio.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/inicio",
      "handler": "inicio.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
